<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.jupitervn.newdatabinding.home.MovieListingViewModel"/>
        <variable
            name="listingVM"
            type="com.jupitervn.newdatabinding.home.MovieListingViewModel" />
    </data>
    <com.jupitervn.newdatabinding.binding.adapter.CustomSwipeRefreshLayout
        android:id="@+id/swipe_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:refreshing="@={listingVM.isPageLoading}"
        tools:context=".home.MainActivity">


        <!--<android.support.v7.widget.RecyclerView-->
        <!--android:id="@+id/rv_movies"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent" />-->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <Button
                    android:id="@+id/bt_stop_refresh"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="StopRefresh"
                    android:onClick="@{(view) -> listingVM.stopRefreshing()}"
                    />
                <Button
                    android:id="@+id/bt_start_refresh"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="StartRefresh"
                    android:onClick="@{(view) -> listingVM.startRefreshing()}"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="10000dp"
                    android:text="ABC"
                    android:gravity="center" />
            </LinearLayout>
        </ScrollView>
    </com.jupitervn.newdatabinding.binding.adapter.CustomSwipeRefreshLayout>
</layout>